# node.jsについて調べた

## ✅ 行ったこと

- daisyUIの導入に出てきたnode.jsについて調べた

## 前回気になった点
daisyUI公式サイトのイストールの場所に書いてあった、「daisyUI導入には、node.jsとtailwindCSSが必要」の中の、node.jsについて調べたくなった

## node.jsとは
詳しく書いてあるサイト(https://qiita.com/non_cal/items/a8fee0b7ad96e67713eb)<br>
ブラウザの中でしか動かなかったJavaScriptをパソコンの中で普通のプログラムとして動かせるようにしようとしたもの

## node.jsをインストールしている部分
dockerfile
```
# Install JavaScript dependencies
ARG NODE_VERSION=20.19.6
ARG YARN_VERSION=1.22.22
ENV PATH=/usr/local/node/bin:$PATH
RUN curl -sL https://github.com/nodenv/node-build/archive/master.tar.gz | tar xz -C /tmp/ && \
    /tmp/node-build-master/bin/node-build "${NODE_VERSION}" /usr/local/node && \
    npm install -g yarn@$YARN_VERSION && \
    rm -rf /tmp/node-build-master
```
ここでやっていることは、
1. AUGで変数を定義
2. node-build ってツールを使って
→ Node.js を /usr/local/node にインストール
3. その Node.js に付属してる npm を使って
→ yarn をグローバルインストール
