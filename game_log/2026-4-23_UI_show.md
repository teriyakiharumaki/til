# 詳細画面のUIデザインの変更

## ✅ 行ったこと

- 詳細画面のUIデザインの変更を行なった

## 変更後の詳細画面のUIデザイン
game_log/resources/views/games/show.blade.php
```
@extends('layouts.app')

@section('content')
  <div class="max-w-3xl mx-auto">
    <div class="card bg-base-100 shadow-xl border border-base-200">
      <div class="card-body">
        <h2 class="card-title text-2xl">
          🎮 {{ $game->title }}
        </h2>

        <div class="divider my-1"></div>

        <div class="grid gap-3 sm:grid-cols-2">
          <div>
            <span class="font-bold">プラットフォーム：</span>
            <span>{{ $game->platform ?: '未登録' }}</span>
          </div>

          <div>
            <span class="font-bold">評価：</span>
            <span>
              @if($game->rating)
                {{ str_repeat('★', $game->rating) }}{{ str_repeat('☆', 5 - $game->rating) }}
              @else
                未登録
              @endif
            </span>
          </div>

          <div>
            <span class="font-bold">ステータス：</span>
            @if($game->status === 'unplayed')
              <span class="badge badge-neutral">未プレイ</span>
            @elseif($game->status === 'playing')
              <span class="badge badge-warning">プレイ中</span>
            @else
              <span class="badge badge-success">クリア済み</span>
            @endif
          </div>

          <div>
            <span class="font-bold">プレイ時間：</span>
            @if($game->play_time_minutes)
              @php
                $h = intdiv($game->play_time_minutes, 60);
                $m = $game->play_time_minutes % 60;
              @endphp

              @if($h > 0)
                {{ $h }}時間
              @endif

              @if($m > 0)
                {{ $m }}分
              @endif
            @else
              未登録
            @endif
          </div>
        </div>

        <div class="mt-4">
          <p class="font-bold mb-2">感想</p>
          <div class="bg-base-200 rounded-lg p-4 whitespace-pre-wrap">
            {{ $game->review ?: '感想はまだありません。' }}
          </div>
        </div>

        <div class="card-actions justify-end mt-6">
          <a href="{{ route('games.index') }}" class="btn btn-outline">
            ← 一覧へ戻る
          </a>
        </div>
      </div>
    </div>
  </div>
@endsection
```

投稿の内容をカードのデザインで表示し、一覧画面でも流用できるように実装

## `<div class="max-w-3xl mx-auto">`

**`max-w-3xl`** → 横幅を制限（広がりすぎ防止）

**`mx-auto`** → 中央寄せ

## `<div class="card bg-base-100 shadow-xl border border-base-200">`(カード全体のデザイン)

**`card`** → DaisyUIのカードUI

**`bg-base-100`** → 背景色（白っぽい）

**`shadow-xl`** → 影（浮き上がる感じ）

**`border`** → 枠線

## `<div class="card-body">`(カードの中身)

**内側の余白（padding）を自動でつける**

## `<h2 class="card-title text-2xl">`

**`card-title`** → カードの見出し用スタイル

**`text-2xl`** → 文字大きく

## `<div class="divider my-1"></div>`

**`divider`** → 横線

**`my-1`** → 上下の余白

## `<div class="grid gap-3 sm:grid-cols-2">`

**`grid`** → グリッドレイアウト

**`gap-3`** → 要素間の余白

**`sm:grid-cols-2`** → スマホで縦1列、PCで2列

## `<span class="font-bold">プラットフォーム：</span>`(ラベル)

**`font-bold`** → 太字

## `str_repeat('★', $game->rating)`(評価を星で表示)

```
4 → ★★★★☆
```

## `<span class="badge badge-success">`(ステータスをバッジで表示)

**`badge`** → ラベルUI

**`badge-success`** → 緑

## `intdiv(...), %`

プレイ時間を時間と分で表示

## `<div class="bg-base-200 rounded-lg p-4 whitespace-pre-wrap">`(レビュー表示)

**`bg-base-200`** → 少しグレー背景

**`rounded-lg`** → 角丸

**`p-4`** → 内側余白

**`whitespace-pre-wrap`** → 改行を保持

## ボタン

### `<div class="card-actions justify-end mt-6">`

**`card-actions`** → ボタン用

**`justify-end`** → 右寄せ

**`mt-6`** → 上に余白

### `<a class="btn btn-outline">`

**`btn`** → ボタン化

**`btn-outline`** → 枠だけ