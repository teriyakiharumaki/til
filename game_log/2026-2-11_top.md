# トップページの作成

## ✅ 行ったこと

- トップページの作成について

## トップページのURLを設定
routes/web.php
```
use Illuminate\Support\Facades\Route;

Route::get('/', function () {
    return view('home');
});
```

(https://qiita.com/rh8_mp1/items/4e0037e8ab2919c9a4d7)

今回はhome.blade.htmlを表示するため、welocomeをhomeに変更

## 表示する画面の作成
resources/views/home.blade.php
```
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Game Log</title>
  </head>
  <body>
    <h1>Game Log</h1>
    <p>ゲームのレビューを記録するアプリ</p>
  </body>
</html>
```

## 共有レイアウトの作成(今後の実装のため)
resources/views/layouts/app.blade.php
```
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@yield('title', 'Game Log')</title>
  </head>
  <body>
    <header>
      <h1>🎮 Game Log</h1>
      <hr>
    </header>

    <main>
      @yield('content')
    </main>
  </body>
</html>
```

### 基本的な部分の解説
```
<!doctype html>
<html lang="ja">
```
- HTML5宣言
- lang="ja" → 日本語ページ

```
<meta charset="utf-8">
```
文字コードUTF-8（日本語を正しく表示するため）

```
<meta name="viewport" content="width=device-width, initial-scale=1">
```
スマホ表示対応

### `<title> の部分`
```
<title>@yield('title', 'Game Log')</title>
```
@yieldは、「ここに、子ビューの内容を差し込む」

子ビュー側で、
```
@section('title', 'トップページ')
```
と書くと、その内容が入る

'Game Log'の部分は初期値で、子ビューが title を指定しなかったら'Game Log'になる

### ヘッダー部分
```
<header>
  <h1>🎮 Game Log</h1>
  <hr>
</header>
```
全ページ共通のヘッダー部分

### mainの中
```
<main>
  @yield('content')
</main>
```
@yield('content') の意味：「各ページの中身をここに表示する」

## トップページをレイアウトに合わせて修正
resources/views/home.blade.php
```
@extends('layouts.app')

@section('title', 'トップページ')

@section('content')
  <p>ゲームのレビューを記録するアプリです。</p>

  <h2>最近のレビュー</h2>
  <ul>
    <li>ゼルダ：★★★★★</li>
    <li>Slay the Spire：★★★★★</li>
  </ul>
@endsection
```

### `@extends('layouts.app')`
**「このビューは layouts/app.blade.php を親として使います」と宣言**

Laravelは、ビューを探すときに、
```
resources/views/
```
を基準にするので、今回の場合
```
@extends('layouts.app')
```
は内部的には
```
resources/views/layouts/app.blade.php
```
を探している

layouts.app の「.」は、フォルダ区切りで
```
layouts.app
↓
layouts/app.blade.php
```
このように変換される

https://zenn.dev/machi1461/articles/adc43af459905d<br>
https://qiita.com/kamome_susume/items/4cb10cf50a090e0d01f0